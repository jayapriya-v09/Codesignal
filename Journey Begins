solution for codesignal:

1-add
C-solution:

     return param1+param2;

2-centuryFromYear
c-solution:
     
     if(year%100==0)
    {
     return year/100;
    }
    else
     {
    return year/100+1;
    }
    
    
3-CheckPalindrome
c-solution:

   int len = strlen(inputString);
 char temp[len+1];
 temp[len] = '\0';
 for(int i = 0; i < len; i++){
    temp[i] = inputString[len-1-i];
 }
 if (strcmp(temp, inputString) == 0)
    return 1;
 else return 0;


